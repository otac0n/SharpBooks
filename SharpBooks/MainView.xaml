<Window x:Class="SharpBooks.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="SharpBooks" Height="450" Width="800">
    <DockPanel LastChildFill="True">
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File" />
        </Menu>
        <TabControl x:Name="TabView" Margin="10,10,10,10">
            <TabItem x:Name="OverviewTabItem" Header="Overview">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Grid x:Name="OverviewGrid">
                    </Grid>
                </ScrollViewer>
            </TabItem>
            <TabItem x:Name="AccountsTabItem" Header="Accounts / Expenses">
                <Grid>
                    <TreeView x:Name="AccountsList" HorizontalContentAlignment="Stretch" Visibility="Visible">
                    </TreeView>
                    <ItemsControl x:Name="SplitList" ItemsSource="{Binding}" Visibility="Hidden">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="{Binding Path=Date}" />
                                    <Label Content="{Binding Path=Value}" />
                                    <Label Content="{Binding Path=IsNegative}" />
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </TabItem>
            <TabItem Header="Payments" Name="PaymentsTabItem">
                <Grid>
                </Grid>
            </TabItem>
            <TabItem Header="Investments" Name="InvestmentsTabItem">
                <Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
